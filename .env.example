# Example environment configuration for the Agentes Voluntários system

# --- Oracle Database (used by backend) ---
ORACLE_DB_HOST=oracle-db
ORACLE_DB_PORT=1521
# Nome do serviço/PDB utilizado pelo backend
ORACLE_DB_SERVICE=XEPDB1
ORACLE_DB_USER=agentes_user
# Senha do usuário do schema utilizado pelo backend (altere!)
ORACLE_DB_PASSWORD=agentes_pass
ORACLE_DB_SCHEMA=AGENTES_USER

# Senha do usuário SYSTEM do Oracle XE (somente para container/healthcheck)
ORACLE_SYS_PASSWORD=OraclePassword123

# --- Keycloak authentication (backend OAuth2 Client) ---
# Nome do realm no Keycloak (ex.: tjmg ou agentes-voluntarios)
KEYCLOAK_REALM=tjmg
# URL pública (acessível pelo navegador)
KEYCLOAK_PUBLIC_BASE_URL=http://localhost:8084/auth
# URL interna (acessível pelos containers)
KEYCLOAK_INTERNAL_BASE_URL=http://keycloak:8080/auth

# Credenciais do cliente OIDC (client-id/secret)
KEYCLOAK_CLIENT_ID=agentes-voluntarios
# Gere/obtenha no Keycloak > Clients > agentes-voluntarios > Credentials
KEYCLOAK_CLIENT_SECRET=d13dc665-5a88-45ae-a163-b9cd3eb69f35

# Credenciais do admin do Keycloak (apenas para container de dev)
KEYCLOAK_ADMIN_USER=admin
KEYCLOAK_ADMIN_PASSWORD=admin

# --- Application settings ---
# Para onde o backend redireciona após login com sucesso (rota do SPA)
FRONTEND_REDIRECT_URI=http://localhost:80/login

APP_BASE_URL=http://localhost
APP_ENVIRONMENT=local
APP_DEV_BASE_URL=http://localhost:8080

# --- Oracle Cloud Infrastructure (optional) ---
OCI_REGION=sa-saopaulo-1
OCI_BUCKET_NAME=agentes-anexos

# --- Frontend runtime API URL (consumido pelo Nginx via envsubst) ---
API_URL=http://backend:8080
